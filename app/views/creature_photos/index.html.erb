<article id="creature-photo-list">
  <header>
    <h1><%= t('.title') %></h1>
  </header>

  <%= render_list class: 'nav nav-tabs' do |li|
    li << [link_to(t('helpers.new'), new_creature_photo_path), { class: 'action' }]
  end %>

  <%= render 'table', creature_photos: @creature_photos %>

  <nav role="pagination">
  </nav>
</article>
