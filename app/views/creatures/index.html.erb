<article id="creature-list">
  <header>
    <h1><%= t('.title') %></h1>
  </header>

  <%= render_list class: 'nav nav-tabs' do |li|
    li << [link_to(t('helpers.new'), new_creature_path), { class: 'action' }]
  end %>

  <%= render 'table', creatures: @creatures %>

  <nav role="pagination">
  </nav>
</article>
